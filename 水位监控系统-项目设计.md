# 水位警戒/监控系统 - 项目设计文档

> 练手目标：覆盖 Spring Boot + MyBatis + Gradle + MariaDB 完整分层架构

---

## 项目简介

模拟真实水位监控场景：多个监测站持续上报水位数据，系统根据预设阈值判断是否触发警报，支持历史记录查询和警报管理。

---

## 技术栈

| 类型 | 技术 |
|------|------|
| 构建工具 | Gradle |
| 框架 | Spring Boot 2.7.x |
| ORM | MyBatis |
| 数据库 | MariaDB |
| 语言 | Java 11 |

---

## 数据库设计

### 1. 监测站表 `station`

```sql
CREATE TABLE station (
    id          BIGINT PRIMARY KEY AUTO_INCREMENT,
    name        VARCHAR(64) NOT NULL COMMENT '站点名称',
    location    VARCHAR(128) COMMENT '地理位置描述',
    warn_level  DECIMAL(6,2) NOT NULL COMMENT '警戒水位(m)',
    danger_level DECIMAL(6,2) NOT NULL COMMENT '危险水位(m)',
    status      TINYINT NOT NULL DEFAULT 1 COMMENT '状态: 1=正常 0=停用',
    created_at  DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at  DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

### 2. 水位记录表 `water_record`

```sql
CREATE TABLE water_record (
    id          BIGINT PRIMARY KEY AUTO_INCREMENT,
    station_id  BIGINT NOT NULL COMMENT '监测站ID',
    water_level DECIMAL(6,2) NOT NULL COMMENT '当前水位(m)',
    recorded_at DATETIME NOT NULL COMMENT '采集时间',
    created_at  DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_station_id (station_id),
    INDEX idx_recorded_at (recorded_at)
);
```

### 3. 警报记录表 `alert_record`

```sql
CREATE TABLE alert_record (
    id          BIGINT PRIMARY KEY AUTO_INCREMENT,
    station_id  BIGINT NOT NULL COMMENT '监测站ID',
    record_id   BIGINT NOT NULL COMMENT '触发的水位记录ID',
    alert_type  TINYINT NOT NULL COMMENT '警报类型: 1=警戒 2=危险',
    water_level DECIMAL(6,2) NOT NULL COMMENT '触发时水位',
    status      TINYINT NOT NULL DEFAULT 0 COMMENT '状态: 0=未处理 1=已处理',
    handled_at  DATETIME COMMENT '处理时间',
    created_at  DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_station_id (station_id),
    INDEX idx_status (status)
);
```

---

## 项目结构

```
water-monitor/
├── build.gradle
├── settings.gradle
└── src/main/
    ├── java/com/example/watermonitor/
    │   ├── WaterMonitorApplication.java      // 启动类
    │   ├── controller/
    │   │   ├── StationController.java        // 站点管理接口
    │   │   ├── WaterRecordController.java    // 水位上报/查询接口
    │   │   └── AlertRecordController.java    // 警报查询/处理接口
    │   ├── service/
    │   │   ├── StationService.java
    │   │   ├── WaterRecordService.java
    │   │   └── AlertRecordService.java
    │   ├── dao/
    │   │   ├── StationDao.java
    │   │   ├── WaterRecordDao.java
    │   │   └── AlertRecordDao.java
    │   ├── mapper/
    │   │   ├── StationMapper.java            // MyBatis Mapper接口
    │   │   ├── WaterRecordMapper.java
    │   │   └── AlertRecordMapper.java
    │   ├── model/
    │   │   ├── pojo/                         // 与数据库表对应的实体
    │   │   │   ├── Station.java
    │   │   │   ├── WaterRecord.java
    │   │   │   └── AlertRecord.java
    │   │   ├── dto/                          // Service层入参出参
    │   │   │   ├── WaterReportDTO.java       // 上报水位入参
    │   │   │   └── AlertHandleDTO.java       // 处理警报入参
    │   │   ├── vo/                           // Controller层返回
    │   │   │   ├── StationVO.java
    │   │   │   ├── WaterRecordVO.java
    │   │   │   └── AlertRecordVO.java
    │   │   └── consts/
    │   │       ├── AlertType.java            // 警报类型枚举
    │   │       └── AlertStatus.java          // 警报状态枚举
    │   └── common/
    │       ├── Result.java                   // 统一返回结果
    │       └── GlobalExceptionHandler.java   // 全局异常处理
    └── resources/
        ├── application.yml
        └── mapper/                           // MyBatis XML文件
            ├── StationMapper.xml
            ├── WaterRecordMapper.xml
            └── AlertRecordMapper.xml
```

---

## 接口设计

### 监测站管理

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/stations` | 获取所有站点列表 |
| GET | `/stations/{id}` | 获取单个站点详情 |
| POST | `/stations` | 新增监测站 |
| PUT | `/stations/{id}` | 修改站点信息（名称/阈值） |
| DELETE | `/stations/{id}` | 停用站点 |

### 水位上报与查询

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/water/report` | 上报水位数据（核心接口） |
| GET | `/water/records` | 查询历史水位（支持站点ID + 时间范围） |
| GET | `/water/latest/{stationId}` | 查询某站点最新水位 |

### 警报管理

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/alerts` | 查询警报列表（支持筛选未处理） |
| PUT | `/alerts/{id}/handle` | 标记警报为已处理 |

---

## 核心业务逻辑

### 水位上报流程（重点练习）

```
POST /water/report
        ↓
  WaterRecordController
        ↓ 调用
  WaterRecordService
    1. 校验站点是否存在
    2. 保存水位记录到 water_record
    3. 查询该站点的 warn_level / danger_level
    4. 判断是否触发警报：
       - water_level >= danger_level → 创建危险警报
       - water_level >= warn_level   → 创建警戒警报
       - 否则不处理
        ↓ 调用
  WaterRecordDao / AlertRecordDao
        ↓
     MariaDB
```

---

## build.gradle 参考

```groovy
plugins {
    id 'java'
    id 'org.springframework.boot' version '2.7.18'
    id 'io.spring.dependency-management' version '1.0.15.RELEASE'
}

group = 'com.example'
version = '0.0.1-SNAPSHOT'
sourceCompatibility = '11'

repositories {
    mavenCentral()
}

dependencies {
    implementation 'org.springframework.boot:spring-boot-starter-web'
    implementation 'org.mybatis.spring.boot:mybatis-spring-boot-starter:2.3.1'
    runtimeOnly 'com.mysql:mysql-connector-j'  // MariaDB 兼容 MySQL 驱动
    compileOnly 'org.projectlombok:lombok'
    annotationProcessor 'org.projectlombok:lombok'
    testImplementation 'org.springframework.boot:spring-boot-starter-test'
}
```

---

## application.yml 参考

```yaml
spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/water_monitor?useUnicode=true&characterEncoding=utf8
    username: root
    password: your_password
    driver-class-name: org.mariadb.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.watermonitor.model.pojo
  configuration:
    map-underscore-to-camel-case: true   # 下划线转驼峰，station_id → stationId

server:
  port: 8080
```

> 注意：使用 MariaDB 原生驱动需在 `build.gradle` 中添加：
> `runtimeOnly 'org.mariadb.jdbc:mariadb-java-client'`
> 并将 `driver-class-name` 改为 `org.mariadb.jdbc.Driver`

---

## 开发顺序建议

1. **建库建表** - 在 MariaDB 中执行上面的 SQL
2. **搭骨架** - 用 Spring Initializr 生成项目，配置 `application.yml` 能连上数据库
3. **Station 模块** - 先做最简单的站点 CRUD，跑通完整分层链路
4. **WaterRecord 模块** - 实现上报接口，暂不加警报逻辑
5. **警报逻辑** - 在上报接口中加入阈值判断和警报创建
6. **Alert 模块** - 实现警报查询和处理接口
7. **优化** - 加分页、加参数校验（`@Valid`）、加日志
8. **Docker 部署** - 打包上传到 Linux，容器化运行

---

## Docker 部署

### 项目根目录新增两个文件

**`Dockerfile`**
```dockerfile
FROM openjdk:11-jre-slim
WORKDIR /app
COPY build/libs/water-monitor-0.0.1-SNAPSHOT.jar app.jar
EXPOSE 8080
ENTRYPOINT ["java", "-jar", "app.jar"]
```

**`docker-compose.yml`**
```yaml
version: '3.8'

services:
  db:
    image: mariadb:10.11
    container_name: water-mariadb
    environment:
      MARIADB_ROOT_PASSWORD: your_password
      MARIADB_DATABASE: water_monitor
    volumes:
      - mariadb_data:/var/lib/mysql
      - ./sql/init.sql:/docker-entrypoint-initdb.d/init.sql  # 自动建表
    ports:
      - "3306:3306"

  app:
    build: .
    container_name: water-app
    depends_on:
      - db
    environment:
      SPRING_DATASOURCE_URL: jdbc:mariadb://db:3306/water_monitor
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: your_password
    ports:
      - "8080:8080"

volumes:
  mariadb_data:
```

> 注意：`application.yml` 中数据库 host 填 `db`（docker-compose 服务名），不是 `localhost`

### 部署步骤

```bash
# 1. 本地打包
./gradlew bootJar

# 2. 上传到 Linux 服务器（替换为你的服务器IP）
scp -r . user@your-server:/home/user/water-monitor

# 3. 在服务器上启动
cd /home/user/water-monitor
docker-compose up -d

# 4. 验证是否启动成功
docker ps
curl http://localhost:8080/stations
```

---

## 学到的知识点覆盖

| 知识点 | 覆盖情况 |
|--------|---------|
| Spring Boot 项目搭建 | ✅ |
| Gradle 依赖管理 | ✅ |
| MariaDB 连接配置 | ✅ |
| Controller / Service / DAO 分层 | ✅ |
| pojo / dto / vo 分离 | ✅ |
| MyBatis XML + 动态SQL | ✅ |
| 枚举类型使用 | ✅ |
| 统一返回结果 | ✅ |
| 全局异常处理 | ✅ |
| 业务逻辑编排（上报→判断→警报） | ✅ |
| Linux 基本操作 | ✅ |
| Dockerfile 编写 | ✅ |
| docker-compose 多容器编排 | ✅ |
| 容器化部署 Spring Boot 应用 | ✅ |

---

*生成日期：2026-03-24*
