spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/water_monitor?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: honghao
    password: 1234
    driver-class-name: org.mariadb.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.watermonitor.model.pojo
  configuration:
    map-underscore-to-camel-case: true

server:
  port: 8080

logging:
  level:
    com.example.watermonitor.mapper: debug
